# 1. 装 PyTorch (Blackwell sm_120 需要 2.8.0+cu128)
pip install torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0 --index-url https://download.pytorch.org/whl/cu128

# 2. 装项目本体
pip install -e . --no-deps

# 3. 装其他依赖
pip install accelerate>=0.25.0 draccus==0.8.0 einops huggingface_hub json-numpy jsonlines \
  matplotlib peft==0.11.1 protobuf rich sentencepiece==0.1.99 timm==0.9.10 \
  tokenizers==0.19.1 transformers==4.40.1 wandb

# 4. tensorflow-cpu (避免抢 CUDA context)
pip install tensorflow-cpu==2.15.0
pip install tensorflow-datasets==4.9.3 --no-deps
pip install tensorflow_graphics==2021.12.3 --no-deps
pip install tensorflow-metadata==1.13.1
pip install protobuf==4.25.9

# 5. dlimp
pip install --no-deps "dlimp @ git+https://github.com/moojink/dlimp_openvla"
装完验证一下：
python -c "
import torch; print('CUDA:', torch.cuda.is_available(), torch.cuda.get_device_name(0))
import tensorflow as tf; print('TF GPUs:', tf.config.list_physical_devices('GPU'))